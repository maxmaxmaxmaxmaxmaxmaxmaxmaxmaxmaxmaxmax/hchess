package com.earthmelon.hchess;

import com.earthmelon.render.Texture;

public enum Piece {
    PAWN(null),
    KNIGHT(null),
    BISHOP(null),
    ROOK(null),
    QUEEN(null),
    KING(null);

    private Piece(Texture texture) {

    }
}
